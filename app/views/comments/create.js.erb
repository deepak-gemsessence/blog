$('.comments').empty();
<% @current_article.comments.each do |comment| %>
  $('.comments').prepend("<%= escape_javascript render(:partial => 'comment', :locals => { comment: comment, article: @current_article }) %>");
<% end %>

$("#body_input").val('');
