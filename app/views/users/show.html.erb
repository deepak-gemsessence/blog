<h1>show user page</h1>

<%= render 'welcome' %>

<table>
  <tr>
    <th>First Name</th>
    <th>Last Name</th>
    <th>Contact No</th>
  </tr>
  <tr>
    <td><%= @user.first_name %></td>
    <td><%= @user.last_name %></td>
    <td><%= @user.contact %></td>
  </tr>
</table>

<%= render 'footer' %>
