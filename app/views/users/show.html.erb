<h1>show user page</h1>

<% if logged_in? %>
  Welcome <%= current_user.first_name %>
<% end %> <br>

<table>
  <tr>
    <th>First Name</th>
    <th>Last Name</th>
    <th>Contact No</th>
  </tr>
  <tr>
    <td><%= @user.first_name %></td>
    <td><%= @user.last_name %></td>
    <td><%= @user.contact %></td>
  </tr>
</table>

<%= render 'footer' %>
